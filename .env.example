# ==============================================================================
# Order Processing Engine - Environment Configuration Template
# ==============================================================================
# Salin file ini menjadi .env di root atau di dalam folder backend/
# Backend membaca variabel lingkungan langsung dari process.env

# ------------------------------------------------------------------------------
# 1. APPLICATION SETTINGS
# ------------------------------------------------------------------------------
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# 2. DATABASE CONFIGURATION (PostgreSQL)
# ------------------------------------------------------------------------------
DATABASE_URL=postgresql://devops_user:devops_pass@localhost:5432/order_processing
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_CONNECTION_TIMEOUT_MS=5000
DB_IDLE_TIMEOUT_MS=30000

# ------------------------------------------------------------------------------
# 3. MESSAGE BROKER & QUEUE (Redis / BullMQ)
# ------------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379
QUEUE_NAME=order-processing
QUEUE_CONCURRENCY=5

# ------------------------------------------------------------------------------
# 4. OBSERVABILITY & TELEMETRY (Prometheus)
# ------------------------------------------------------------------------------
METRICS_ENABLED=true
METRICS_PREFIX=ope_

# ------------------------------------------------------------------------------
# 5. CROSS-ORIGIN RESOURCE SHARING (CORS)
# ------------------------------------------------------------------------------
CORS_ORIGIN=http://localhost:5173
